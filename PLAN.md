# voksa — Phase Plan and Live Status

Source: docs/research/03-implementation-playbook.md (full acceptance criteria there).
This file is the LIVE status tracker. Claude Code updates it after every milestone.
Rule: main is always green. Phases are sequential; do not start N+1 before N is tagged.

## Status legend
- [ ] not started   [~] in progress   [x] done (tagged)

## Phases

| # | Phase | Status | Tag | Listening CP | Completed (date / SHA) | Deviations |
|---|-------|--------|-----|--------------|------------------------|------------|
| 0 | Repo scaffolding: workspace, Nix flake, CI, hooks, verifier subagent, eSpeak oracle xtask | [x] | phase0-complete | — | 2026-07-02 / 6256d1e | flake: crane check + lld override dropped (stale sketch); voksa-web wasm-opt flags (-Oz + bulk-memory/nontrapping-fptoint/sign-ext); fmt hook = fmt --all; oracle WAVs gitignored; CI validated by inspection (not yet pushed) |
| 1 | Engine spike: klattsch-core integration, hardcoded vowel → WAV, FFT formant harness | [x] | phase1-complete | — | 2026-07-03 / f9e53cf | 2 new crates (voksa-engine-klattsch std adapter + voksa-testkit dev harness) — klattsch-core is std-only, cannot live in no_std core; klattsch pinned =0.1.1; project sample rate 48 kHz; PhonemeTable trait is std-side → gate input: own IR in core, adapter lowers |
| 2 | Lojban phoneme table: 17 C + 6 V + 16 diphthongs, LojbanTable: PhonemeTable | [x] | phase2-complete | — | 2026-07-03 / ca86dac | Own IR in voksa-core instead of klattsch's lossy PhonemeTable trait; loqa-voice-dsp broken → hand-rolled LPC in testkit; Klatt alternating A2 polarity + gain 1.0 in lowering; measurement F0 105 grid + whitened harmonic FFT interpolation; GATE: klattsch KEPT (ADR 0001), Phase-10 = vendored fork for OQ/DI |
| 3 | Syllabifier + word classifier (pure fns, proptest, CLL vectors) | [x] | phase3-complete | — | 2026-07-03 / ae01af9 | CLL-text corrections folded into phonology.md: forbidden triples ndj/ndz/ntc/nts added; classifier = ANY pair (not permissible; CLL §4.3 bisycla) + ends-in-y→cmavo; deterministic syllabic-sonorant rule (vowel-less regions only) + onset maximization documented as the chosen CLL-valid variants |
| 4 | Stress + pause insertion (penultimate w/ exclusions; mandatory + --dotside) | [x] | phase4-complete | — | 2026-07-03 / 335975f | phonology.md gained two MISSING mandatory rules (CLL §4.9 r4 pause-before-cmevla w/ la-family exemption; §4.2 .y. double pause) + y-final generalization (§17.2) + §4.2 stressed+stressed rule; iy/uy ruled uncountable; capitals map char→syllable, brivla misplacement = error |
| 5 | Schedule compiler: text → deterministic param schedule; --buffer flag | [x] | phase5-complete | — | 2026-07-03 / d1bc60f | Engine-neutral IR realized in core (schedule.rs: Frame limited to current engine vocabulary — OQ/TL/FL/DI deferred to Phase-10 fork); adapter reduced to 1:1 lowering (gain + A2-polarity quirks isolated); Phase-2 snapshots byte-identical through the refactor; buffering = fully-buffered dialect minus trailing buffer; every written period honored as pause; syllabic nuclei reuse consonant steady spec (revisit at CP1) |
| 6 | Number/lerfu normalization (PA cmavo, pi, ki'o, hex; by/.abu/.y'y) | [x] | phase6-complete | — | 2026-07-03 / 48c3e15 | ki'o emitted as FULL 3-digit groups (elision never emitted); hex = vocabulary only, not auto-detected (letters in figures = typed error); ":"→pi'e; h/q/w lerfu resolved per CLL §17.5 (.y'y.bu / ky.bu / vy.bu); lerfu written dots reproduced by existing pause rules — zero new pause code |
| 7 | Prosody layer: declination, stress realization, xu rise | [x] | phase7-complete | **CP1** | 2026-07-03 / 4a40e53 | pitch-detection 0.3 REJECTED by smoke gate (formant-locks ~490 Hz on the buzzy source; clarity/power gates reject everything at recommended settings) → hand-rolled NSDF in testkit (lag range 70–200 Hz, 0.8 peak gate, parabolic refinement, 5-pt median); xu rise also raises later in-span events (a following vowel event re-set F0 down); acoustic RMS check = peak 30 ms window (stop closures stretch); human tags after listening |
| 8 | Native CLI adapter: cpal + rtrb, offline WAV mode | [x] | phase8-complete | — | 2026-07-03 / b800d85 | cpal 0.18 (Apache-2.0) + rtrb 0.3; whole-utterance render → SPSC ring → callback only pops + zero-fills (no alloc, verified by a hand-rolled counting allocator since assert_no_alloc is BSD-1-Clause); hand-rolled RIFF writer (hound stays dev-only); render at the negotiated device rate (no resampling); flake gains alsa-lib + pkg-config + LD_LIBRARY_PATH, CI gains libasound2-dev; playback degrades gracefully to a WAV-render hint when headless |
| 9 | Web adapter: wasm-bindgen, AudioWorklet, demo page, size budget | [x] | phase9-complete | — | 2026-07-03 / 1e00312 | Option B (wasm instantiated IN the worklet, per CLAUDE.md). voksa-web dropped the wasm-bindgen RUNTIME dep for a raw C-ABI (voksa_alloc/dealloc/render/out_len/free_f32) → the `.wasm` declares ZERO imports, so the worklet uses `new WebAssembly.Instance(module, {})`; wasm-bindgen-test stays a dev-dep only. simd128 via .cargo/config.toml + `--enable-simd` wasm-opt flag (else silent wasm-opt skip). `cargo xtask wasm-size` gzips (~33 KB < 43 KB budget) + asserts zero imports (wasm-dis, best-effort). chromium+chromedriver in the flake; the headless-chrome test is ADVISORY (continue-on-error) — the wasm-bindgen-test-runner↔chromedriver handshake resets in WSL though the driver works standalone; identical synth logic is covered by required native unit tests |
| 10 | Attitudinal layer: F0/voice-quality overlay, OQ/DI, cai/sai/ru'e/nai | [~] awaiting CP2 | phase10-complete | **CP2** | 2026-07-04 / (pending human tag) | Vendored klattsch fork (`crates/klattsch-core-fork/`) adds OQ (Rosenberg Tp scale) + DI (alternate-cycle F0/2 subharmonic), default byte-identical; vibrato/tilt already upstream. Frame gains oq/tilt/di/vibrato_hz lanes (Frame::modal + NEUTRAL_* + guard); `attitudinal.rs` = 7 kinds (ui/uu/oi/ii/o'o/au/o'onai) × invented deviation vectors (additive Hz, no_std), cai/sai/ru'e/nai intensity, preceding-word scope; compile detects → `UtteranceSchedule.attitudinals`; `apply_attitudinal` overlays on prosody. Adapter Option-gates VQ against the prev frame (modal → all-None byte-identical, colored word resets on exit); `render_utterance_prosodic` runs the overlay (native/CLI/browser, no C-ABI change). testkit: measure_spectral_tilt/f0_variance/diplophonia. wasm 34965 B gzip zero-imports. STOP at CP2 — human tags. |
| 11 | Polish: naturalness levers 1–5, fuzzing, docs, WIT component, release prep, final battery | [~] awaiting CP3 | v0.1.0 | **CP3** | 2026-07-05 / (pending human tag) | Naturalness N-A…N-D (defaults ON — the one deliberate sound change) + W1 fuzz (2 real bugs found+fixed) + W2 rustdoc gate + W3 wasip2 component (ADR 0002) + W4 v0.1.0 release prep + W5 final battery ALL SHIPPED; adversarial review confirmed 2 defects, both fixed red-first (see log). STOP: human scores artifacts/listening/phase11/ and tags v0.1.0 — never the agent. |

## Decision gates
- End of Phase 2: klattsch-core sufficient? (formant tolerances hit; OQ/diplophonia
  path feasible). If NO → switch to hand-rolled cascade/parallel engine or fundsp;
  record as ADR in docs/adr/.
- Phase 9: WASM size budget blown? → twiggy profile, panic=abort, dep audit
  BEFORE adding features.
- Any phase: browser CI flaky? → pin Chrome in the Nix flake; browser test becomes
  advisory, native tests remain required.

## Naturalness backlog (rules-only; owner-approved 2026-07-03)

Clarified intent: "robotic" in the original brief meant MECHANICAL AND
ALGORITHM-DRIVEN (deterministic, no ML, no recorded data) — NOT "must sound
like a robot". The v1 monotone baseline is a deliberate starting point, not
the target. Ceiling for rules-only ≈ high-end 1990s formant TTS (DECtalk
class); neural-TTS naturalness is out of scope by design. Levers, in impact
order:

1. **Voice source realism** — LF-model glottal pulse, open quotient (OQ),
   spectral tilt (TL), aspiration noise in vowels (AH, breathiness). Lands in
   **Phase 10** (vendored klattsch glottal fork): use these for the DEFAULT
   voice baseline, not only the attitudinal overlay.
2. **F0 flutter/jitter** — Klatt FL-style slow quasi-random F0 wobble via a
   SEEDED PRNG (deterministic ≠ constant; schedule stays reproducible and
   snapshot-testable). Baseline-on in **Phase 10** (FL is already in the
   overlay param set).
3. **Microprosody** — consonant-driven F0 perturbations (dips around voiced,
   rises after voiceless obstruents), intrinsic vowel F0/duration. NEW work —
   schedule-compiler transform, post-Phase-10 (fold into Phase 11 or a new
   phase at owner's call).
4. **Duration rules** — phrase-final lengthening, cluster shortening (Klatt
   1976 duration model). NEW work, same placement as (3).
5. **Deeper coarticulation** — duration-dependent formant target undershoot
   on top of the existing consonant loci. NEW work, same placement as (3).

All five stay inside the project constraints: pure Rust, deterministic
pipeline, CLL-only linguistics, no training data.

### CP1 findings (Phase 7 listening, 2026-07-03) → queued fixes

Rater: dhilipsiva. Caveat recorded in docs/listening/phase7.md: self-taught
Lojban (never heard another speaker), scored with eSpeak as the reference.
MOS intelligibility avg 2.6, naturalness avg 2.3; ABX: flat preferred 8/10.

6. ✅ **DONE (Phase 7.1, 2026-07-03)** — **Nucleus-scoped stress stretch.**
   The whole stressed span (incl. onset cluster transitions) stretched ×1.5 →
   "CC strong + long" on gismu-initial stressed syllables (pre/kla/zga/dja/
   DJO), which explained the 8/10 flat ABX preference. FIXED: `SyllableSpan`
   now carries `nucleus_off_ms`; the stretch window opens at the nucleus so
   only the rhyme stretches — onsets keep unit rate. Excursion + amplitude
   stay whole-span. Snapshots regenerated, battery re-rendered; awaiting a
   quick owner ABX re-check (the fix is committed, NOT re-tagged — phase7
   stays the tag).
7. **Segment tuning** — coi-munje heard as "soi-oon-shae" (MOS 1/1): /c/ [ʃ]
   not distinct enough from [s], nasal /m/ murmur weak, /j/ [ʒ] reads
   devoiced. Phoneme-table work (docs/formants.md), independent of prosody.
8. **Oracle comparability** — eSpeak jbo found WRONG twice: xu rendered
   [k]-like (CLL x = [x], German Bach — voksa judged better by the rater)
   and raw-digit reading of "4" in "li 3.14" (voksa's CLL §18 expansion to
   "vo" is correct). Battery tweak: also feed eSpeak the normalized
   PA-cmavo string for number utterances so the oracle column stays
   comparable; never treat eSpeak as ground truth (fixture policy already
   says regression-oracle-only).

## Distribution / packaging backlog (post-v0.1, owner-requested 2026-07-04)

- ~~**Port the demo UI to Dioxus.**~~ **DONE — tagged `console-v1` (2026-07-05).**
  `crates/voksa-console` is a Dioxus 0.7 component library over the `voksa-web`
  synth surface; `crates/voksa-console-demo` is the `dx serve` runner. Deviation
  from the sketch here: the engine is linked as a plain **rlib** (ADR 0003),
  not synthesized in the worklet — `synth`/`transcription`/`default_params` are
  direct Rust calls, and the AudioWorklet is a player-only Blob-URL worklet.
  Dioxus does pull in wasm-bindgen (bundle ~357 KB gzip vs the 42 KB voksa-web
  cdylib), gated separately by `cargo xtask console-size`. The legacy
  `www/index.html` page was retired in C6; the voksa-web C-ABI stays as the
  non-Rust embedding surface. See the C0–C6 session-log entry below.
- **Refactor to a `cargo add`-able crate.** Make the synth a published library
  (`cargo add voksa` / `voksa-core`): stabilize the public API, set
  `publish = true` + real versioning/semver, add crate-level docs + examples,
  and audit the dependency/licence surface for crates.io. Currently the crates
  are workspace-internal (0.0.1, unpublished).

## Session log
(append: date, phase, sessions used, notes)

- 2026-07-05 — **Console port C0–C6 (Dioxus tuning console), tagged `console-v1`** — owner commissioned a ground-up UI/UX redesign against their QUINE design system (handoff bundle from Claude Design); this ports it to a Dioxus 0.7 component-library crate consumable by dhilipsiva.dev as a git dependency. Interlude after Phase 11; does NOT touch the CP3/v0.1.0 flow (battery pages are xtask artifacts, not the demo). Owner decisions: **engine-in-app** (Option A — the Dioxus wasm links `voksa-web` as a plain rlib, direct `synth`/`transcription`/`default_params` calls, player-only AudioWorklet; ADR 0003), **retire the classic page immediately**, **author the ~74 help entries in this port**. **C0** vendored the design bundle to `docs/design/tuning-console/` + ADR 0003. **C1** (pure-Rust model, red-first): the frozen 449-float `Path` space (Display/FromStr bijection), `Descriptors::from_defaults(voksa_web::default_params())` (never hand-copied), widen-never-clamp, f32-space dirty diffing, delta-only `export` + REPLACE `load`, presets; the config round-trip is compiler-checked against voksa-cli's REAL `Config` parser; sentences.json + synth gate moved into the crate. **C2** reactive store (`Vec<Signal<Cell>>` + `generation` counter — one slider re-renders one row), the `vx-*` stylesheet on QUINE tokens, layout + racks A/B + ParamRow's four states + MatrixCell, the `voksa-console-demo` `dx serve` runner (flake already had dioxus-cli). **C3** engine facade + audio: player-only worklet (`include_str!`, Blob-URL `addModule`, 48 kHz ctx, gesture-gated), main-thread render, live transcript memo (tokenized colored spans), SVG waveform, WAV download — all wasm-gated behind a native stub so `model/` tests stay native. **C4** deep editors: 41-keycap phoneme grid → per-phoneme editor (Steady/Stop/Dur dispatch), 7 attitudinal emotion chips → 8-field editor + try-example, the A/B naturalness latch (`ab_effective` render-time override, values preserved). **C5** the share loop (export/download + load via file-picker OR drag-drop, REPLACE + widen, bad-JSON crimson status) + `?` help popovers (`help_for` registry, ~74 authored entries, completeness-gated `tests/help.rs`) + `λ` about panel; verified live in the browser (drop a config with `declination_start_hz=400` → widened not clamped; export → delta-only JSON with stamps; bad JSON → state intact). **C6** ship: (a) retired `crates/voksa-web/www/{index.html,voksa-processor.js,sentences.json}` + `tests/{version.rs,sentences.rs}` + the unused serde/serde_json dev-deps (version parity now BY CONSTRUCTION via `env!` in the console; sentence gate lives in `voksa-console/tests/sentences.rs`); (b) `cargo xtask console-size` (wipes stale dx artifacts → release `dx build` → gzip the bundle wasm → 465 KB canary = ~1.3× the 356 993 B measured baseline; SKIPS the zero-imports check since the Dioxus bundle legitimately imports js-sys/web-sys) + a `console` CI job (dioxus-cli pinned to the workspace dioxus 0.7.9 so dx's wasm-bindgen matches); (c) rewrote `docs/handoff-dhilipsiva-dev.md` (git-dep Dioxus component PRIMARY — dep line + `TuningConsole` mount + props + the QUINE-token/manganis CSS contract + the demo as reference; raw C-ABI demoted to SECONDARY for non-Rust hosts), README `dx serve` quickstart + architecture, CLAUDE current-state + Commands + Workspace map. Full workspace green after the retire (365 tests, was 367); console-size passes (356 993 < 465 000); native+wasm clippy + fmt clean; dx build OK. Adversarial review (5 lenses × 2-refuter) + fresh-context verifier before the tag. `TuningConsole { initial_theme, class, inline_styles }` is the mount API. `.claude/launch.json` (untracked local) serves the debug build for preview.

- 2026-07-05 — Phase 11 infra W1–W5 + ship (same session as N-A…N-D below) → **awaiting CP3**. **W1 stable fuzzing**: proptest suites `crates/voksa-core/tests/fuzz.rs` (arbitrary-unicode totality, lojban/number soup → structural `assert_sane` with a RELATIVE span epsilon — sub-eps slack scales under legitimate extreme rates — plus compile/transcribe Ok-agreement, determinism, hostile-but-finite VoiceTables) and `crates/voksa-web/tests/fuzz.rs` (hostile f32 blocks incl. NaN/inf through the full synth render at 8 kHz; render-bound → case count self-caps at 1024, printed not silent). The pre-registered `scale_rate` hole fixed red-first (NaN slips both guards; subnormal rates overflow k=1/rate; huge-k total overflow — all identity now). The 65536-case deep run then caught a SECOND real bug: hostile-but-finite amp × aspiration products (−7.2e27 × 3e26 in /n/'s slots, minimizer-found) exceed f32::MAX → inf → NaN, poisoning the filter state for the rest of the render → non-finite samples now degrade to silence at `render_schedule`, the ONE boundary all three surfaces share (finite renders byte-identical; zero snapshot drift). Also fixed: proptest's default SourceParallel failure-persistence silently fails for integration-test targets ("failed to find lib.rs") — the original seed was LOST and had to be re-minimized; the web suite pins `FileFailurePersistence::Direct` and the seed file is committed. `cargo xtask fuzz` (PROPTEST_CASES=65536, `--cases N`) + weekly `fuzz-deep` CI job. **W2 docs**: `#![warn(missing_docs)]` on the four shipping crates — the plan's 15–25 estimate was off 7×: 169 items, 173 doc comments written (delegated agent, spot-checked); the new `docs` CI job (`RUSTDOCFLAGS=-D warnings cargo doc --no-deps`, fork/testkit/xtask excluded) also surfaced 42 pre-existing bare-IPA `[h]`-style doc links rustdoc parses as broken intra-doc links — backticked. **W3 WIT component** (ADR 0002): `crates/voksa-component` (publish=false) exposes `voksa:synth@0.1.0` (synthesize/transcribe/version; `flag-bits` — `flags` is a WIT keyword) via `wit_bindgen::generate!` in an `#[allow(missing_docs)]` bindings module, delegating to `voksa_web::synth`/`transcription` — parity by construction + native parity tests; `cargo xtask component` = wasm32-wasip2 release build → `wasm-tools validate` → WIT-drift needles → 200 KB gzip canary (measured 62 KB); flake += wasip2 rust-std + wasm-tools; `component` CI job; wit-bindgen's `Apache-2.0 WITH LLVM-exception` recorded as owner-approved in the ADR. **W4 release prep**: version 0.1.0 (red-first `voksa-web/tests/version.rs` parity test on the demo's `voksaVersion` config stamp), root LICENSE-MIT/LICENSE-APACHE, `crates/klattsch-core-fork/LICENSE` = upstream MIT (Tony Gies) — the crates.io tarball omitted it, a REAL compliance gap while redistributing the fork; publish=false on all 8 crates (the fork's name collides with upstream); README + CHANGELOG. **W5 final battery**: `cargo xtask final-battery` → artifacts/listening/phase11/ = 10 CP1 items × {default release voice, naturalness-off (frozen Phase-10 voice), flat, raw oracle, + normalized-PA-cmavo oracle for digit items via unit-tested `normalized_words` over `compiler::tokenize` (CP1 finding 8)} + 7 attitudinal pairs × {affect, base, oracle}, merged MOS/ABX page; template docs/listening/phase11.md. **Ship review** (21-agent workflow: 5 lenses × 2-refuter): 8 raw findings, 2 CONFIRMED, both fixed red-first — (1) `apply_undershoot` treated the microprosody settle insert (an F0-only copy of its parent vowel) as its own vowel, computing a DEEPER u from the vowel's remainder → spurious mid-vowel schwa glide + "stressed nuclei not at all" violated on every stressed post-obstruent vowel in the default voice; missed because every undershoot test had obstruent_f0_hz=0 and every settle test had undershoot=0 → run-scoped rewrite (one u per same-class run; single-event runs bit-identical; exactly 7 snapshots value-regenerated, sadness_mi_klama correctly unmoved — no post-obstruent vowels; FROZEN_OK); (2) demo `applyConfig` merged instead of replaced the 16 knob keys + flags — a pre-P11 7-key config loaded over the "Naturalness off" preset played/re-exported a voice `voksa --config` never produces → replace semantics (reset bounds + default when absent), verified live in the browser. 6 findings refuted (fuzz-coverage critiques + a stale-handoff-doc note, all judged non-shipping; handoff-doc refresh queued as post-release backlog). 338 tests; wasm 42 029 B gzip < 43 KB zero imports; component 62 KB < 200 KB. Battery re-rendered at the corrected voice; verifier pass; **CP3 STOP — the human scores and tags v0.1.0**.

- 2026-07-05 — Phase 11 naturalness (N-A…N-D, all four chunks red-first, interim-pushed) — owner approved FULL scope (levers 1–5 + WIT component); Phase 10 stays untagged (CP2 delegated to community), Phase 11 proceeds by owner decision. **N-A engine flutter**: fork `Params.flutter` (22nd param; default 0 byte-identical, guarded by a full-render identity test) implements Klatt-1990 FL — ΔF0 = (FL/50)(F0/100)[sin 2π12.7t + sin 2π7.1t + sin 2π4.7t] clocked off `sample_clock/sr` (block-size invariant, deterministic — SUPERSEDES the backlog's seeded-PRNG idea; no state to seed). testkit += `measure_f0_track_raw` (no median filter — the 5-pt median kills 12.7 Hz) + `f0_band_rms` (detrend via fit_line, DFT sinusoid power over 3–15 Hz, self-tested on a synthetic FM pulse train); adapter acoustic test: FL=50 raises band RMS >3×. ParamUpdate field-addition regenerated 5 engine snapshots (`flutter: None` lines only — plan said zero churn, accepted as pure field-addition per P10 precedent). **N-B IR metadata + micro + durations** (identity defaults, zero value drift): `Event.micro: MicroClass` (field, NOT a parallel vec — two transforms insert events) with `Vowel(High/Mid/Low)/Diphthong/Voiced-/VoicelessObstruent/Sonorant/Aspirate/Silence`; `SyllableSpan.onset_count/coda_count` post-buffering via a consecutive-run scan keyed (span, is_coda) that RESETS on non-consonant entries — buffers break clusters (the naive count called buffered v+r a 2-cluster); `Frame.flutter` lane. Implemented `apply_cluster_shortening` (onset window ×max(1−r(k−1), 0.6), remaps nucleus_off), `apply_microprosody` (intrinsic vowel F0 ±Δ; post-voiceless +Δ / voiced −0.6Δ with a settle EVENT inserted at +50 ms via partition_point; pauses block), `apply_final_lengthening` (last span's rhyme window, composes with stress ×1.5·1.3=1.95 — exact test). Generalized `stretch_windows` piecewise time-map (bit-identical refactor of the stress stretch). **N-C undershoot + VQ stage + 449 crossing**: `apply_undershoot` (short-monophthong formants → schwa center (500,1500,2500) by u·max(0,1−d/200ms); class-RUN duration scan so microprosody settle-inserts can't fake short vowels; stressed/diphthongs spared — buffered [ɪ] F2 → 1784.5 exact test), `apply_naturalness` (flutter/OQ/tilt baselines + voiced-only breath, attitudinal clamps), adapter flutter Option-gating (modal still all-None), web block 440→449 (`FULL_PARAM_COUNT`; old 7/63/440 blocks stay valid), CLI nine flat JSON keys, demo naturalness slider group (Basic tab, seeded from `voksa_default_params`) + "Naturalness off" preset + ?v=8. Pipeline: cluster→micro→stretch→declination→excursion→final→undershoot→xu→VQ→rate (undershoot BEFORE xu — the insert would fake a short final vowel; durations at prosody time — flat path stays pure). **N-D defaults ON** (the sound-change commit): the nine pinned constants flip to flutter 25 / breath 0.06 / OQ +0.10 / tilt −0.10 / micro-F0 4 Hz / obstruent 6 Hz / final ×1.3 / cluster 0.15 / undershoot 0.35; `naturalness_off()` = the frozen Phase-10 voice, guarded by the never-regenerated `naturalness__snapshot_naturalness_off_coi_munje`. Phase-7 exact-arithmetic contract tests (schedule + acoustic) pinned to naturalness_off — settle inserts + final lengthening break their event-count/span math by design (PLAN-GAP found at red time: the plan named only the acoustic trio; the schedule-level prosody.rs tests needed the same pin). New default-voice acceptance: 3–15 Hz band RMS default > off ×1.5 on cmavo-only text (isolates levers from stress), declination slope still negative, stress F0 prominence survives ±4 Hz micro-F0. VALUE regen of exactly 8 snapshots (3 prosody + 5 attitudinal); FROZEN_OK. phonology.md §9.2 documents all nine + order. 322 tests; wasm 41854 B gzip < 43 KB, zero imports. Next: W1 fuzz → W2 docs → W3 WIT component → W4 release prep → W5 final battery → review/verifier → CP3 STOP (human tags v0.1.0).

- 2026-07-05 — Demo tuning console D2c (phonetic transcription display) — owner: "ignore CP2, my actions — I'm not good at this; show the syllables so people can tell us if something has wrong phonetics." **CP2 validation is DELEGATED to the community** — the demo displays voksa's own analysis so Lojban speakers judge phonetics against what they hear; the owner tags phase10-complete on community confirmation. Red-first (11 vectors, all green first pass). Core: `compile_with`'s front half extracted verbatim into pub `utterance_items()` (promoted `UtteranceItem`; `detect_attitudinals` reads Word items with identical indices) — the SINGLE pipeline both audio and display consume, zero snapshot drift; `transcribe::transcribe` renders it with CLL-flavored notation (syllable dots, CAPITALS for the stressed syllable, ` ‖ ` merged pauses, apostrophe [h], comma→dot, inline `(ɪ)` buffers mirroring the compiler's cluster-consonant adjacency — a between-syllable buffer lands before the dot: `MUN(ɪ).je`; numbers show their normalized cmavo: `li 3.14` → `li ci pi pa vo`). Web: `transcription(text, flags)` + `voksa_transcribe` C-ABI (UTF-8, voksa_dealloc contract); UI shows `→ …` + legend under the sentence picker, refreshed on every text/flag mutation path, and config exports gain `phonetics` so bug reports carry what the tuner saw. Adversarial review (2 lenses × 2 refuters; parity probes incl. cross-syllable buffer + attitudinal indices under pauses) confirmed ONE bug, fixed: the att-try button set the text programmatically (no `input` event) leaving the line stale while the audio spoke the example. Also a gate-script lesson: a piped `grep` masked a clippy failure into a green commit — one style follow-up (needless `mut`). 294 tests; wasm 38049 B gzip, zero imports. Tagged demo-phonetics.

- 2026-07-05 — Demo tuning console D2b (Advanced tab: FULL per-phoneme VoiceTable + phonetic sentence picker) — owner: "ignore CP2, give the community the knobs… even pick different phonetic sentences"; chose EVERYTHING tunable + a dropdown-with-cycle picker. Red-first per chunk. **Core**: `data::` seed helpers became `const fn` and `VoiceTable::PINNED` is built FROM them (one copy of the numbers — default byte-identity by construction, zero snapshot regen, insta 0 pending); `spec`/`buffer_spec`/`compile` are thin delegates over `spec_with`/`buffer_spec_with`/`compile_with`; layout = 377 floats (vowels 6×12 @0, diphthong durs 16 @72, stops 6×24 @88, fricatives 7×12 @232, nasals @316, liquids @340, h @364, buffer @365 — diphthong glide endpoints DERIVE from tuned vowels, [h] shapes from the next vowel); all durations clamp `.max(0.0)` (negative JSON durations can't walk the schedule backwards; NaN can't panic the span sort). **Web**: f32 block 63→440; per-index finite-fallback decode; **CLI**: `phonemes` delta-merge (per-letter keys, nested closure/burst `TargetsOverride` for stops, dur_ms-only for diphthongs/'/buffer) through `--config`. **UI**: six descriptor-generated panel sections (377 sliders, delegated readouts/resets, delta-only export, widen-not-clamp import). **Sentence picker**: `www/sentences.json` (18 curated coverage sentences — clusters, sibilants, cmevla pauses, capital stress, xu, numbers, buffer, coda cluster, syllabic r, triple onset, all-6-vowels, all-6-stops, [h], a diphthong sweep built ONLY from the 9 unrealized diphthong cmavo so no attitudinal overlay fires, attitudinal demo) + dropdown + ⏭ cycle, gated by a native test that SYNTHESIZES every entry (green first run). **Defaults-from-wasm**: the adversarial-review fleet died on a session limit (its "no findings" was vacuous — recorded honestly), so instead of re-reviewing the 377 hand-copied JS default literals we removed the drift CLASS: new `voksa_default_params` C-ABI export returns the canonical 440-float block; the demo instantiates the module on the main thread and re-seeds every slider descriptor from it (literals remain only as a no-wasm fallback). The re-run adversarial fleet (18 agents, 2-refuter verification; layout/value parity of all 377 crossings independently confirmed clean, including a Node execution of the page's own descriptor JS against the real `PINNED.to_array()`) confirmed TWO defects, both fixed red-first: (1) durations were clamped below but UNBOUNDED above — a loadable `dur_ms: 1e30` config aborted the CLI on a 824 GB allocation and wedged the demo at "speaking…" → `clamp_dur` (0–10 000 ms per segment, identity for pinned) + `render_ms` (~10-minute cap at all three render sites) + a `node.onprocessorerror` handler; (2) the voice panels assigned wasm-read f64-of-f32 defaults via raw `.value` (step-snap) so a FRESH console exported dozens of spurious overrides and readouts showed `0.8499999642…` → `f32nice` shortest-round-trip normalization + exact-set passes + `Math.fround`-space delta comparison. 283 tests; wasm 36884 B gzip, zero imports. Tagged demo-advanced after the fresh-context verifier pass.

- 2026-07-04 — Demo tuning console D2a (Advanced tab: attitudinals) — owner-requested community playground ("give lojban community users… json export from advanced tab"), shipped while CP2 listening is pending (defaults untouched → battery stays valid). Red-first: `AttitudinalTable` (runtime copy of the 7 deviation vectors; `Default` = `PINNED`, guarded by `default_table_matches_pinned_vectors`) + `apply_attitudinal_with` (the pinned `apply_attitudinal` is now a thin wrapper — zero snapshot drift) + `AttitudinalKind::ALL/index/cmavo` + `Deviation::to_array/from_array` as the ONE canonical flat-f32 order; engine `render_utterance_expressive` (prosodic delegates with default). Web: f32 block 7 → 63 (`FULL_PARAM_COUNT`; indices 7..63 decoded per-index with finite-fallback to pinned) — 7-float demo-basic and empty blocks stay valid forever; wasm 35121 B gzip, still zero imports. CLI: `attitudinals` JSON override block (per-cmavo keys ui/uu/oi/ii/o'o/au/o'onai, per-field Options, missing = pinned) threaded through `--config`. UI: 7 collapsible panels, Lojban cmavo label + English sub-text, 8 sliders each (JS descriptors mirror the core order + pinned values verbatim), try-it example phrases, per-panel reset, delta-only JSON export. ULTRACODE adversarial review (16 agents, 4 lenses × 2-refuter verification) found 1 real bug, confirmed 3×: applyConfig let range inputs silently CLAMP out-of-range loaded values (hand-edited config f0_mean_hz=60 → web played 40, readout lied 60, re-export rewrote 40 while `voksa --config` played 60) — fixed by `setSliderExact` (widen bounds + relax step grid on load, readout always shows the slider's real value; reset restores descriptor bounds; also fixed the same latent D1 pattern on the basic sliders). 269 tests green. Owner follow-up shipped in the same milestone: debounced AUTO-SPEAK — any input change (text/flags/all 63 sliders/preset/reset/load-config) re-speaks after 400 ms via a "speak on change" toggle (default ON); playback is live-REPLACED (liveNode disconnect), never stacked; every wired event is a user gesture so the AudioContext unlock composes. Tagged demo-attitudinal. D2b (per-phoneme VoiceTable) still TODO.

- 2026-07-04 — Phase 10 — attitudinal layer, COMMITTED NOT TAGGED (CP2 STOP — human tags after listening). Red-first per chunk. **P10-1 fork**: vendored klattsch-core 0.1.1 verbatim (MIT, Tony Gies) into `crates/klattsch-core-fork/` (workspace dep → path; name/version kept), then added two default-preserving glottal lanes — OQ scales the Rosenberg open phase `Tp` (clamped [0.05,0.7]; OQ=1.0 byte-identical, guarded) and DI dips the excitation on odd glottal cycles via a `glottal_parity` flag (F0/2 subharmonic; DI=0 unchanged); both ramp through `for_each_param!`. **P10-2 core**: Frame += oq/tilt/di/vibrato_hz (Frame::modal + NEUTRAL_* consts + guard); `attitudinal.rs` = 7 kinds (ui→Joy, uu→Sadness, oi→Complaint, ii→Fear, o'o→Patience, au→Desire, o'onai→Anger) × invented deviation vectors expressed as **additive Hz** (voksa-core is no_std — no transcendentals, unlike the semitone plan), cai/sai/ru'e/nai intensity (nai = −1 polarity flip), scope = nearest preceding non-marker word (utterance-initial → first content word); `compile` detects → `UtteranceSchedule.attitudinals`; `apply_attitudinal` re-centers the F0 excursion by range×, adds the mean shift, sets the VQ lanes + breathiness, applies the dominant rate as a global tempo scale. 5 per-attitudinal snapshots. **P10-3 adapter**: `lower_events` Option-gates VQ against the PREVIOUS frame (seeded with modal defaults) — modal utterances stay all-None (engine schedule snapshots byte-identical) AND a colored word resets to modal on exit (no bleed, the subtle correctness point); `render_utterance_prosodic` runs `apply_attitudinal` after `apply_prosody` (native/CLI/browser, no C-ABI change). testkit += measure_spectral_tilt / measure_f0_variance / measure_diplophonia (twin-lag NSDF). Engine-direct acoustic tests confirm the fork DSP: DI → F0/2 subharmonic, OQ → spectral-tilt shift (empirically low OQ *darkens* — direction not asserted, only a measurable shift), vibrato → F0 oscillation, determinism. **P10-4**: `cargo xtask attitudinal-battery` renders 7 items × 3 (affect / neutral-base / eSpeak oracle) to artifacts/listening/phase10/ (gitignored) + scoring index.html; docs/listening/phase10.md template; phonology.md §10; ADR 0001 Phase-10 path marked DONE. Core snapshot regen throughout = pure field-addition (verified line-by-line, zero value drift). 257 workspace tests; wasm 34965 B gzip, zero imports. CP2 pending.

- 2026-07-04 — Demo tuning console D1 (Basic) — interlude between Phases 9 and 10 (owner-requested crowdsourced tuning: fiddle → download JSON → share → replay). Made the 6 prosody knobs + a global `rate` runtime fields on `ProsodyOptions`, defaulted to the pinned constants so all 12 snapshots + acoustic tests stay byte-identical (guarded by `default_options_equal_pinned_constants`); `apply_prosody` reads the fields and adds a rate scale (1.0 = exact identity). Web: `voksa_render_params` C-ABI takes an f32 param block (JS writes it via `voksa_alloc`, like the text) → the wasm stays import-free at ~33 KB gzip (serde deliberately NOT in the wasm — it would blow the 43 KB gate). Demo `www/index.html`: tabbed console (Basic active, Advanced = D2 placeholder), 7 sliders + 4 flags + text driven by one descriptor list that IS the f32 layout + CLI key schema; presets, reset, Download/Load config JSON, Download WAV (JS RIFF encoder mirroring wav.rs), waveform canvas, notes field; worklet postMessages the PCM back to the main thread. Round-trip: `voksa --config <file.json>` (voksa-cli += serde/serde_json, native-only) maps text + flags + params onto the option structs and replays exactly. Also fixed the Phase-9 demo bug found on first listen: the worklet encoded text with `TextEncoder`, absent from Firefox's AudioWorkletGlobalScope → moved encoding to the main thread + `?v=` cache-bust. Red-first per chunk (core/web/cli). Tests: 198/198; wasm import-free + under budget. Tag `demo-basic`. D2 (Advanced tab: runtime `VoiceTable` for per-phoneme acoustics) still TODO.

- 2026-07-03 — Phase 9 — web adapter (Option B: wasm synthesized INSIDE the AudioWorklet, honoring the CLAUDE.md directive). Key finding: exposing the engine via a raw C-ABI (`#[unsafe(no_mangle)] extern "C"`, edition-2024 syntax) with NO wasm-bindgen runtime yields a `.wasm` with an EMPTY import section, so the worklet instantiates it synchronously with `new WebAssembly.Instance(module, {})` — no glue, no ESM-in-worklet problem. voksa-web now depends on voksa-engine-klattsch (verified wasm-compilable: klattsch-core has zero deps, no std::time/thread/random); wasm-bindgen-test is the only wasm-bindgen-family dep and it's dev-only (its `#[wasm_bindgen_test]` harness needs no `#[wasm_bindgen]` on the tested fn). Surface: `synth(text,flags,sr)->Result<Vec<f32>>` shared by 5 raw exports (alloc/dealloc/render/out_len/free_f32; length via an AtomicUsize register — clippy-clean vs `static mut`) + native unit tests. Worklet renders the whole utterance once in its constructor, copies PCM out of wasm memory (re-reading `.buffer` after each allocating call — grow detaches views), plays 128-frame chunks alloc-free. Demo: www/index.html (compileStreaming on main → processorOptions → AudioWorkletNode). Build plumbing: simd128 via .cargo/config.toml `[target.wasm32-unknown-unknown]` (target-scoped, native untouched) + `--enable-simd` in voksa-web's wasm-opt flags (without it wasm-pack 0.15 silently ships the unoptimized binary); `cargo xtask wasm-size` (real) gzips the wasm (33_091 B) vs a 43_000 B budget and asserts zero imports via wasm-dis (best-effort if binaryen absent); CI wasm-build now runs `cargo xtask wasm-size` (hard gate, + binaryen for wasm-dis). Browser test: chromium+chromedriver added to the flake; `wasm-pack test --headless --chrome` — chromedriver + chromium work standalone (manual W3C session succeeds) but the wasm-bindgen-test-runner gets "connection reset" from the driver it spawns in WSL (not IPv6 — all of 127.0.0.1/localhost/::1 reach it; wasm-bindgen 0.2.126 is current so not the old race). Per the CLAUDE.md gate the CI wasm-test job is `continue-on-error: true` (advisory); native voksa-web unit tests (identical `synth`) + the import-free/size gate are the required coverage. 187/187 workspace tests, wasm import-free at ~33 KB gzip. Tagged phase9-complete.

- 2026-07-03 — Phase 8 — native CLI. `voksa-cli` gains a lib (args/wav/playback) + thin bin. `voksa [FLAGS] <text>` plays; `--out FILE` renders a 48 kHz mono WAV (never touches the audio device — CI-safe); flags `--flat --xu --dotside --buffer`. Playback: render the whole utterance up front → prefill an rtrb SPSC ring → cpal output callback only pops + fans mono across channels + zero-fills underruns (docs/research/02-architecture-v2.md). Render happens at the *negotiated device rate* (schedule is in ms, so exact, not resampling). Realtime-safety proven by a hand-rolled counting `#[global_allocator]` in tests/realtime.rs (thread-local FORBID flag; `assert_no_alloc` is BSD-1-Clause, banned) — the callback path allocates 0×. Hand-rolled 44-byte RIFF writer keeps hound a dev-dep. 20 new tests (7 args, 2 wav + 1 hound round-trip, 4 realtime, 6 e2e via CARGO_BIN_EXE); 184/184 workspace. Build plumbing: cpal 0.18 (Apache-2.0) + rtrb 0.3 (MIT/Apache); flake devshell += alsa-lib, pkg-config, LD_LIBRARY_PATH; CI clippy+test jobs += libasound2-dev (wasm-build builds only voksa-web, untouched). API notes: cpal 0.18 `sample_rate()` returns u32 (no `.0`), `build_output_stream` takes `config` by value. Live playback bounded by an audio-length+2 s deadline so a broken device can't hang; degrades to the friendly "use --out" message when headless (WSL/CI). Tagged phase8-complete.

- 2026-07-03 — Phase 7.1 — nucleus-scoped stress stretch (CP1 backlog item 6). `SyllableSpan` gained `nucleus_off_ms` (onset consonants + [h] + onset-side buffer, computed in schedule_word via an is_nucleus flag on Entry); prosody's `stretch_stressed_spans` now uses a rhyme-only window `[start+nucleus_off, end)` while the F0 excursion + amplitude boost stay whole-span. Onsets keep unit rate (e.g. coi-munje "mun" 465→425 ms, total 1055→1015). 165/165 tests (6 new compiler/prosody + 2 rewritten to rhyme arithmetic); 12 core snapshots regenerated (8 compiler + normalize field-only, 3 prosody field+timing), engine snapshots byte-identical (lowering reads events, not spans). Battery re-rendered. Committed, NOT re-tagged (phase7-complete stands); owner to ABX-recheck the onset-cluster items.

- 2026-07-03 — Phase 7 CP1 — human sign-off: owner tagged phase7-complete (at e3f3c74). Scores in docs/listening/phase7.md: MOS int avg 2.6 / nat avg 2.3, ABX flat preferred 8/10. Dominant artifact = whole-span stress stretch lengthening onset clusters ("CC strong + long" on pre/kla/zga/dja/DJO) — the §9.1 stop-burst caveat confirmed; nucleus-only stretch queued (backlog item 6). Segmental c/m/j clarity issues on coi-munje (item 7). Rater caught eSpeak being WRONG twice (xu → [k]-like; raw-digit "4") — voksa correct per CLL; oracle-comparability tweak queued (item 8). Rater caveat recorded: self-taught Lojban, eSpeak-referenced scoring.
- 2026-07-03 — Phase 7 — 1 session — Prosody green, 156/156 tests (15 new: 10 schedule-level + 3 insta snapshots in core, 3 acoustic in the adapter, plus testkit F0 self-tests). Deterministic transform: stretch stressed spans 1.5× (piecewise time map; pauses shift, not stretch) → additive declination 120→95 → +20 Hz/×1.2 in-span → optional xu +25 Hz. Measured: declination slope negative with endpoints 120±8/95±8; stressed F0 > unstressed +5 Hz and peak-window RMS higher; xu ending >110 Hz vs flat ~98. pitch-detection 0.3 failed the smoke gate (McLeod formant-locked ~490 Hz) → dropped for a hand-rolled NSDF (plan's designated fallback). Fixed during green: xu rise must also raise later events inside the final span. Battery: 10 utterances × 3 WAVs (prosodic/flat/oracle) + index.html with MOS/ABX capture, clipping assert clean. COMMITTED, NOT TAGGED — CP1 human listening pending.
- 2026-07-02 — Phase 0 — 1 session — Workspace (4 crates), flake (fenix stable 1.96.1 + wasm32 std, espeak-ng), CI, portable Windows/WSL hooks, verifier subagent, skills, working oracle (coi-munje.wav RIFF-valid, 58690 B). TDD smoke: red (wrong assertion) → Stop hook blocked exit 2 → fix → 7/7 green → Stop hook passed. Found+fixed: wasm-pack 0.15 silently swallows wasm-opt failures (binaryen 129 rejects rustc's default bulk-memory ops) via per-crate wasm-opt feature flags; wasm 17171 → 15923 B. Fresh-context verifier: all criteria PASS.
- 2026-07-02 — policy — Auto-push enabled at owner request: PostToolUse hook pushes `origin HEAD --follow-tags` after every git commit/tag. Guard hook now blocks only force pushes and stray rm -rf (push-to-main block removed).
- 2026-07-03 — Phase 1 — 1 session — Engine spike green: klattsch-core 0.1.1 renders steady /a/ measured at F1 719.6 / F2 1079.6 / F3 2519.9 Hz vs targets 730/1090/2440 (1.4% / 1.0% / 3.3% error — inside ±10% gate, near ±5% Phase-2 band). FFT harness (voksa-testkit, spectrum-analyzer 1.7 + hound dev-only) self-tested on synthetic tones. Red-first honored: acceptance test committed failing against a silent skeleton, then implementation. Gate input for Phase 2: klattsch PhonemeTable trait is std-side — plan is own schedule IR in voksa-core, adapter lowers to klattsch; OQ/diplophonia params still absent from klattsch (known, decides at gate). 13/13 tests, clippy/fmt clean, a.wav rendered.
- 2026-07-03 — Phase 6 — 1 session — Normalization green, 141/141 tests (14 new: CLL §18 number vectors incl. 18.3.7 verbatim, round-trip proptest, lerfu table + spell, pause-reproduction of written lerfu forms, end-to-end figures). Proptest caught a real bug pre-commit: the canonical-grouping length check wrongly applied to ungrouped digit runs ("0000" rejected — would have broken CLL 18.2.3's ten-digit string). One test-expectation fix: no pause is mandated between abu and by (neither rule fires) — implementation was right. Tokenizer gained decimal lookahead (period between digits stays in the figure).
- 2026-07-03 — Phase 5 — 1 session — Schedule compiler green, 127/127 tests (20 new: 8 insta utterance snapshots, tokenizer/error vectors, determinism, buffer invariants incl. CLL 8.1 vrusi, writer-period merge). The Phase-2 gate architecture landed: core owns the IR + timing (schedule.rs/compiler.rs), the adapter shrank to a 1:1 event translation with klattsch quirks isolated — proven behavior-preserving by the Phase-2 snapshots passing byte-identical (zero pending) through the refactor. One test-helper fix during green: voiceless stop closures are acoustically silence, so pause counting discriminates by hold length (100 ms vs 60 ms). render_utterance gives end-to-end text→audio.
- 2026-07-03 — Phase 4 — 1 session — Stress + pauses green, 107/107 tests (19 new: CLL stress vectors incl. all §4.8 capital-marked names, complete §4.9 pause ruleset, 2 proptests). Research found phonology.md missing TWO mandatory pause rules (r4 pause-before-cmevla unless la/lai/la'i/doi — classic CLL, not dotside; .y. trailing pause) — both implemented + doc-fixed. iy/uy diphthongs ruled uncountable (CLL §3.4 phonetics + §3.9 weak-stress). One test-vector correction during red→green: e'U-initial utterances open with the r3 vowel-initial pause (CLL writes .e'u) — test amended pre-commit with citation, implementation untouched. Capital→syllable mapping via the Phase-3 round-trip guarantee.
- 2026-07-03 — Phase 3 — 1 session — Syllabifier + classifier green, 88/88 tests (41 new: letters/clusters units, ~30 CLL vectors, 5 proptest properties). Implementation passed the full CLL vector set on the FIRST green attempt (armstrong rm|str split, kat,r,in syllabic R, brlgan forced sonorants, ktraile typed error, bisycla impermissible-pair classification). CLL research corrected phonology.md: forbidden triples ndj/ndz/ntc/nts (§3.7) were missing; classifier needs ANY pair not a permissible one (§4.3); syllabicity is speaker-choice (§3.4) → voksa's deterministic rule documented. proptest 1.11 added (dev-only).
- 2026-07-03 — Phase 2 — 1 session — Full inventory green, 47/47 tests. All 6 vowels within ±5% (F1 floor 50 Hz) by BOTH FFT and LPC (e.g. /a/ LPC 725/1081/2432 vs 730/1090/2440); sibilant centroids s 6363 / z 6436 / c 3425 / j 3291 / x 2458 Hz all in-band; /ai/ F2 glide monotonic 1080→2280. Debug journey worth remembering: (1) klattsch default gain 3.5 soft-clips → corrupted LPC + intermodulation ghosts — adapter renders at gain 1.0; (2) loqa-voice-dsp 0.5 empirically broken (garbage on synthetic vowels) — LPC hand-rolled in testkit (Levinson-Durbin + Durand-Kerner); (3) parallel-topology spectral zeros biased LPC F2 +100–170 Hz — fixed by Klatt-1980 alternating A2 polarity in the lowering (errors → single-digit Hz); (4) FFT harmonic-grid quantization — measurement F0 105 Hz + source-whitened log-parabolic interpolation across ±F0 neighbours. ENGINE GATE: klattsch-core KEPT (ADR 0001); Phase-10 OQ/DI via vendored fork of its glottal source.
